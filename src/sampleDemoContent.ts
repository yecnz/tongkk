// '예시로 둘러보기' 데모용 정적 콘텐츠 — 서버/AI 호출 없이 번들에 포함된다(게스트도 열람 가능).
// 주제: 컴공 공감형 "시간 복잡도(Big-O)와 정렬 알고리즘". 원본 → 4종 요약 → 샘플 퀴즈로
// 핵심 흐름(자료 → 요약 → 자료 이해 → 퀴즈)의 산출물을 업로드 전에 미리 체험시킨다.
// 설계 근거: notes/tutorial-redesign-plan.md 부록 A.
// 수식은 remark-math + rehype-katex가 렌더한다(LaTeX 명령은 \\ 로 이스케이프).

import type { NodeData } from "./components/mindmapData";

export const SAMPLE_TOPIC = "시간 복잡도와 정렬 알고리즘";

export const SAMPLE_SOURCE_NAME = "알고리즘 3주차 강의자료.pdf";

/** 사용자가 '올린' 원본 강의 자료(가정). */
export const sampleOriginalMarkdown = `# 시간 복잡도와 정렬 알고리즘

## 1. 시간 복잡도와 Big-O 표기
알고리즘의 효율은 입력 크기 $n$이 커질 때 연산 횟수가 어떻게 늘어나는지로 평가한다. 이를 점근적 상한으로 나타낸 것이 **Big-O 표기**다.

대표적인 증가율은 다음 순서로 커진다.

$$O(1) < O(\\log n) < O(n) < O(n \\log n) < O(n^2)$$

- $O(1)$: 입력 크기와 무관 (예: 배열 인덱스 접근)
- $O(\\log n)$: 매 단계 문제 크기가 절반 (예: 이진 탐색)
- $O(n \\log n)$: 효율적인 비교 기반 정렬의 한계
- $O(n^2)$: 이중 반복문 (예: 단순 정렬)

## 2. 대표 정렬 알고리즘
- **버블 정렬**: 인접한 두 원소를 비교·교환한다. 평균 $O(n^2)$, 안정 정렬.
- **선택 정렬**: 매번 최솟값을 찾아 앞으로 보낸다. 평균 $O(n^2)$, 불안정.
- **삽입 정렬**: 정렬된 부분에 하나씩 끼워 넣는다. 거의 정렬된 입력에 강하고 안정적이다.
- **병합 정렬**: 반으로 나눠 정렬한 뒤 합친다. 항상 $O(n \\log n)$, 안정, 추가 공간 $O(n)$.
- **퀵 정렬**: 피벗 기준으로 분할한다. 평균 $O(n \\log n)$, 최악 $O(n^2)$, in-place.
- **힙 정렬**: 힙 자료구조를 이용한다. 항상 $O(n \\log n)$, in-place, 불안정.

## 3. 어떤 정렬을 언제 쓰나
- 데이터가 작으면 구현이 쉬운 삽입 정렬도 충분하다.
- 안정성이 필요하면(같은 값의 원래 순서 유지) 병합 정렬을 쓴다.
- 메모리가 빠듯하면 추가 공간이 적은 퀵·힙 정렬이 유리하다.
`;

export type SampleSummaryKind = "GENERAL" | "LECTURE_NOTE" | "MINDMAP" | "CHEAT_SHEET";

export const sampleSummaries: { kind: SampleSummaryKind; label: string; markdown: string }[] = [
  {
    kind: "GENERAL",
    label: "일반 요약",
    markdown: `# 시간 복잡도와 정렬 알고리즘

## 1. 시간 복잡도와 Big-O 표기
- **시간 복잡도**: 입력 크기 $n$이 커질 때 연산 횟수가 늘어나는 정도.
- **Big-O 표기**: 증가율의 점근적 상한을 나타낸다.
- 대표 증가율(작은 것부터): $O(1) < O(\\log n) < O(n) < O(n \\log n) < O(n^2)$.

### 1.1 증가율별 의미
- $O(1)$: 입력 크기와 무관 — 배열 인덱스 접근.
- $O(\\log n)$: 매 단계 문제 크기가 절반 — 이진 탐색.
- $O(n \\log n)$: 효율적인 비교 기반 정렬의 한계.
- $O(n^2)$: 이중 반복문 — 단순 정렬.

## 2. 정렬 알고리즘의 분류

    정렬 알고리즘
    ├─ 단순 정렬 (O(n^2))
    │  ├─ 버블 정렬
    │  ├─ 선택 정렬
    │  └─ 삽입 정렬
    └─ 고급 정렬 (O(n log n))
       ├─ 병합 정렬
       ├─ 퀵 정렬
       └─ 힙 정렬

## 3. 알고리즘별 특징
### 3.1 단순 정렬
- **버블 정렬**: 인접한 두 원소를 비교·교환. 평균 $O(n^2)$, 안정.
- **선택 정렬**: 매번 최솟값을 골라 앞으로. 평균 $O(n^2)$, 불안정.
- **삽입 정렬**: 정렬된 부분에 하나씩 삽입. 거의 정렬된 입력에 강함, 안정.

### 3.2 고급 정렬
- **병합 정렬**: 분할 후 병합. 항상 $O(n \\log n)$, 안정, 추가 공간 $O(n)$.
- **퀵 정렬**: 피벗 기준 분할. 평균 $O(n \\log n)$, 최악 $O(n^2)$, in-place.
- **힙 정렬**: 힙 이용. 항상 $O(n \\log n)$, in-place, 불안정.

## 4. 알고리즘 선택 기준
> - 데이터가 작으면 구현이 쉬운 삽입 정렬도 충분하다.
> - 안정성이 필요하면 병합 정렬을 쓴다.
> - 메모리가 빠듯하면 추가 공간이 적은 퀵·힙 정렬이 유리하다.`,
  },
  {
    kind: "LECTURE_NOTE",
    label: "강의 노트",
    markdown: `# 시간 복잡도와 정렬 알고리즘

## 한눈에 보는 흐름
- 알고리즘 효율을 입력 크기 $n$ 기준으로 비교할 필요에서 출발한다.
- 증가율을 **Big-O**로 표기해 알고리즘을 객관적으로 비교한다.
- 같은 정렬 문제도 알고리즘에 따라 $O(n^2)$에서 $O(n \\log n)$까지 효율이 갈린다.
- 안정성·메모리 제약을 함께 고려해 상황에 맞는 정렬을 고른다.

## 핵심 개념
### 시간 복잡도
- 정의: 입력 크기 $n$에 대한 연산 횟수의 증가율.
- 의미: 실제 실행 시간이 아니라 입력이 커질 때의 경향을 본다.
- 중요한 이유: 하드웨어와 무관하게 알고리즘을 비교할 수 있다.

### Big-O 표기
- 정의: 증가율의 점근적 **상한**을 나타내는 표기.
- 예시: $O(1) < O(\\log n) < O(n) < O(n \\log n) < O(n^2)$.
- 적용: 최악의 경우를 보수적으로 가늠할 때 쓴다.

### 안정 정렬
- 정의: 값이 같은 원소의 원래 순서가 정렬 후에도 유지되는 정렬.
- 중요한 이유: 다중 기준 정렬에서 앞선 정렬 결과를 보존한다.
- 예시: 버블·삽입·병합은 안정, 선택·퀵·힙은 불안정.

## 방법/절차
1. 데이터 크기를 확인한다 — 작으면 삽입 정렬로 충분하다.
2. 안정성이 필요한지 판단한다 — 필요하면 병합 정렬.
3. 메모리 여유를 확인한다 — 빠듯하면 in-place인 퀵·힙 정렬.
4. 최악의 경우를 따져 본다 — 퀵 정렬은 최악 $O(n^2)$에 주의.

## 주요 용어
| 용어 | 설명 | 헷갈리는 점 |
| --- | --- | --- |
| in-place | 추가 메모리를 거의 쓰지 않고 정렬 | 병합 정렬은 in-place가 아님 |
| 안정 정렬 | 같은 값의 상대 순서를 유지 | 선택 정렬은 불안정 |
| 점근적 상한 | 입력이 커질 때 증가율의 한계 | 실제 속도와는 다를 수 있음 |

## 시험 포인트
> 평균 시간 복잡도가 $O(n \\log n)$인 정렬을 모두 고르면? (출처: 알고리즘 3주차 강의자료 p.2)
> - 답:
>   - 병합 정렬
>   - 퀵 정렬(평균)
>   - 힙 정렬

> 퀵 정렬이 최악에 $O(n^2)$이 되는 경우는? (출처: 알고리즘 3주차 강의자료 p.3)
> - 답:
>   - 피벗이 항상 최솟값이나 최댓값으로 치우칠 때
>   - 이미 정렬된 입력에 단순 피벗을 쓸 때

## 핵심 암기 사항
> - **항상 $O(n \\log n)$**: 병합·힙 정렬
> - **최악 $O(n^2)$ 주의**: 퀵 정렬
> - **안정 정렬**: 버블·삽입·병합
> - **추가 공간 $O(n)$**: 병합 정렬

## 참고/주의 사항
> - 병합 정렬은 안정적이지만 추가 메모리가 필요하다.
> - 퀵 정렬은 평균은 빠르지만 피벗 선택에 따라 최악이 나빠진다.
> - 평균 복잡도와 최악 복잡도를 섞어 외우지 않도록 주의한다.`,
  },
  {
    kind: "MINDMAP",
    label: "마인드맵",
    markdown: `## 정렬 알고리즘

- **분류 기준**
  - 비교 기반 vs 비비교
  - 안정 vs 불안정
  - in-place 여부
- **단순 정렬 ($O(n^2)$)**
  - 버블 정렬 (안정)
  - 선택 정렬 (불안정)
  - 삽입 정렬 (안정)
- **고급 정렬 ($O(n \\log n)$)**
  - 병합 정렬 (안정, 추가 공간 $O(n)$)
  - 퀵 정렬 (in-place, 최악 $O(n^2)$)
  - 힙 정렬 (in-place, 불안정)`,
  },
  {
    kind: "CHEAT_SHEET",
    label: "치트시트",
    markdown: `## 정렬 알고리즘 비교표

| 알고리즘 | 평균 | 최악 | 안정성 | in-place |
| --- | --- | --- | --- | --- |
| 버블 | $O(n^2)$ | $O(n^2)$ | 안정 | O |
| 선택 | $O(n^2)$ | $O(n^2)$ | 불안정 | O |
| 삽입 | $O(n^2)$ | $O(n^2)$ | 안정 | O |
| 병합 | $O(n \\log n)$ | $O(n \\log n)$ | 안정 | X |
| 퀵 | $O(n \\log n)$ | $O(n^2)$ | 불안정 | O |
| 힙 | $O(n \\log n)$ | $O(n \\log n)$ | 불안정 | O |

## 빠른 암기
- **항상 $O(n \\log n)$**: 병합, 힙
- **최악 $O(n^2)$ 주의**: 퀵
- **안정 정렬**: 버블, 삽입, 병합`,
  },
];

// 마인드맵 예시 — 실제 MINDMAP 템플릿은 JSON 트리를 MindmapView로 렌더한다(중첩 불릿이 아님).
export const sampleMindmap: NodeData = {
  label: "정렬 알고리즘",
  children: [
    { label: "분류 기준", children: [
      { label: "비교 기반 vs 비비교", children: [] },
      { label: "안정 vs 불안정", children: [] },
      { label: "in-place 여부", children: [] },
    ] },
    { label: "단순 정렬 O(n²)", children: [
      { label: "버블 (안정)", children: [] },
      { label: "선택 (불안정)", children: [] },
      { label: "삽입 (안정)", children: [] },
    ] },
    { label: "고급 정렬 O(n log n)", children: [
      { label: "병합 (안정·추가 공간)", children: [] },
      { label: "퀵 (in-place·최악 n²)", children: [] },
      { label: "힙 (in-place·불안정)", children: [] },
    ] },
  ],
};

export type SampleQuizQuestion = {
  type: "객관식" | "OX" | "단답형" | "주관식";
  question: string;
  options?: string[];
  /** 객관식: 정답 보기 인덱스 */
  answerIndex?: number;
  /** OX·단답형·주관식: 정답(또는 모범답안) 텍스트 */
  answer?: string;
  explanation: string;
};

export const sampleQuiz: SampleQuizQuestion[] = [
  {
    type: "객관식",
    question: "평균 시간 복잡도가 $O(n \\log n)$이 **아닌** 정렬은?",
    options: ["병합 정렬", "퀵 정렬", "힙 정렬", "선택 정렬"],
    answerIndex: 3,
    explanation: "선택 정렬은 평균·최악 모두 $O(n^2)$이다.",
  },
  {
    type: "OX",
    question: "퀵 정렬은 입력과 무관하게 항상 $O(n \\log n)$이다.",
    answer: "X",
    explanation: "피벗이 한쪽으로 치우치면 최악 $O(n^2)$이 된다.",
  },
  {
    type: "단답형",
    question: "병합 정렬이 추가로 사용하는 공간 복잡도는?",
    answer: "$O(n)$",
    explanation: "병합 과정에서 입력 크기에 비례하는 임시 배열이 필요하다.",
  },
  {
    type: "객관식",
    question: "안정 정렬(stable)이 **아닌** 것은?",
    options: ["삽입 정렬", "병합 정렬", "선택 정렬", "버블 정렬"],
    answerIndex: 2,
    explanation: "선택 정렬은 교환 과정에서 같은 값의 상대 순서가 바뀔 수 있어 불안정하다.",
  },
  {
    type: "주관식",
    question: "퀵 정렬과 병합 정렬을 **안정성·메모리** 관점에서 비교해 설명하시오.",
    answer: "병합 정렬은 안정 정렬이지만 추가 공간 $O(n)$이 필요하고, 퀵 정렬은 in-place라 메모리 효율이 좋지만 불안정하며 최악에 $O(n^2)$이다.",
    explanation: "주관식은 AI가 이런 모범답안과 비교해 점수·피드백을 줍니다.",
  },
];
