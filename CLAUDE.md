## Working Directory
Always run `pwd` and `git status` at the start of any session involving git operations or file changes. Confirm the correct repository before executing commands, especially when multiple related repos exist (e.g., tongkk vs tongkk-langgraph).
